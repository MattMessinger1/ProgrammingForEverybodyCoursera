#Exercise 3.3 Write a program to prompt for a score between 0.0 and 1.0. 
#If the score is out of range, print an error message. 
#If the score is between 0.0 and 1.0, print a grade using the following table:
# Score Grade >=0.9 A >=0.8 B >=0.7 C >=0.6 D <0.6 F

fscore = raw_input("Enter score here between 0.0 and 1.0:") 

try: 
	score = float(fscore)
	
except: 
	print "Please enter a number between 0.0 and 1.0. Thanks!"
	exit()
	
if 1.0 > score >= 0.9: 
	print "A"
elif 0.9 > score >= 0.8: 
	print "B"
elif 0.8 > score >= 0.7: 
	print "C"
elif 0.7 > score >= 0.6: 
	print "D"
elif score < 0.6: 
	print "F"
	
else: 
	print "Please enter a score between 0.0 and 1.0." 
